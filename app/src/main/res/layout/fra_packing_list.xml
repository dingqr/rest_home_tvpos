<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/ll_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/color_e9e9e9"
    android:orientation="vertical">

    <com.yonyou.framework.library.widgets.ESwipeRefreshLayout
        android:id="@+id/srl_packing"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="@dimen/px_20"
        android:background="@drawable/bg_paking_list">

        <com.yonyou.framework.library.widgets.pla.PLALoadMoreListView
            android:id="@+id/pla_lv_packing"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:cacheColorHint="@android:color/transparent"
            android:divider="@android:color/transparent"
            android:fadingEdge="none"
            android:fadingEdgeLength="0dp"
            android:listSelector="@android:color/transparent"
            android:overScrollMode="never"
            android:scrollbars="none"
            app:plaColumnNumber="1"
            app:plaContentBackground="@color/color_FFFFFF" />
    </com.yonyou.framework.library.widgets.ESwipeRefreshLayout>

</LinearLayout>